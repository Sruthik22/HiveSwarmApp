<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="beekeepersjournal.beekeepersjournal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="beekeepersjournal.beekeepersjournal.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <!-- Network State Permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <service
        android:name=".HiveBackgroundService"
        android:exported="false"/>

    <receiver android:name=".Alarm_Receiver" />

    <service
        android:name=".RingtonePlayingService"
        android:enabled="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GeneralObservationsActivity" />
        <activity android:name=".EnvironmentalConditions" />
        <activity android:name=".PestsDiseases" />
        <activity android:name=".EggQueenObservations" />
        <activity android:name=".Feeding" />
        <activity android:name=".Home">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".SetAlarm" />
        <activity android:name=".PreferencesView" />
        <activity android:name=".PreferencesChange" />
        <activity android:name=".GraphActivity" />
        <activity android:name=".GraphDataActivity">

        </activity>
    </application>

</manifest>